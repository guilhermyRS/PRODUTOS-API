<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= product.name %> - Detalhes</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @layer utilities {
      .glass-effect {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
      }
    }
  </style>
</head>
<body class="bg-gray-900 text-gray-200 font-sans">
  <!-- Header -->
  <header class="bg-gray-800 shadow-md sticky top-0 z-10">
    <div class="container mx-auto p-4 flex justify-between items-center">
      <h1 class="text-2xl font-bold text-yellow-400">Loja Virtual</h1>
      <nav>
        <ul class="flex space-x-6">
          <li><a href="/" class="hover:text-yellow-400">Início</a></li>
          <li><a href="/cart" class="hover:text-yellow-400">Carrinho</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <!-- Detalhes do Produto -->
  <main class="container mx-auto py-8 px-4">
    <div class="flex flex-col lg:flex-row items-center gap-8">
      <!-- Imagem -->
      <div class="w- lg:w-1/2">
        <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-auto object-contain rounded-md">
      </div>

      <!-- Informações -->
      <div class="w-full lg:w-1/2 glass-effect p-6 rounded-lg">
        <h2 class="text-3xl font-bold mb-4"><%= product.name %></h2>
        <p class="text-gray-400 text-sm mb-4"><%= product.description %></p>
        <p class="text-green-400 text-2xl font-bold mb-4">R$ <%= product.price %></p>
        <p class="text-sm mb-4"><%= product.freeShipping ? 'Frete Grátis' : 'Sem Frete Grátis' %></p>
        <form action="/add-to-cart" method="POST" class="flex flex-col gap-4">
          <input type="hidden" name="productId" value="<%= product.id %>">
          <label for="quantity" class="text-sm">Quantidade:</label>
          <input type="number" name="quantity" id="quantity" value="1" min="1" required
                 class="bg-gray-800 border border-gray-700 rounded p-2 text-gray-200">
          <button type="submit" class="bg-yellow-500 text-gray-900 py-2 rounded hover:bg-yellow-400">
            Adicionar ao Carrinho
          </button>
        </form>
        <a href="/" class="block text-center text-yellow-400 mt-4 hover:underline">
          Voltar para a Home
        </a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 py-4 text-center">
    <p class="text-sm text-gray-400">Loja Virtual &copy; 2024</p>
  </footer>
</body>
</html>
